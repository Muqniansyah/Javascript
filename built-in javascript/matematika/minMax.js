// Math.min(...args), Math.max(...args): Mengembalikan nilai minimum atau maksimum dari serangkaian angka.

const minimumValue = Math.min(3, 1, 4, 1, 5, 9, 2);
console.log(minimumValue); // Output: 1

const maximumValue = Math.max(3, 1, 4, 1, 5, 9, 2);
console.log(maximumValue); // Output: 9
