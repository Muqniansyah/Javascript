// contoh symbol
// symbol digunakan untuk menunjukkan identifier yg unik. untuk membuatnya bisa berikan deskripsi/nama symbol

const id1 = Symbol("id");
const id2 = Symbol("id");

console.log(id1 == id2);

// symbol umumnya digunakan untuk nama property dari object
