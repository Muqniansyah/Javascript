// forEach: forEach digunakan untuk melakukan iterasi melalui setiap elemen dalam array dan menjalankan fungsi yang diberikan pada setiap elemen.

const fruits = ["apel", "pisang", "ceri"];
fruits.forEach((fruit) => console.log(fruit));
// Ini akan mencetak setiap buah ke konsol.
