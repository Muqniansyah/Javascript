// Spreading operator dituliskan dengan three consecutive dots (...).
// fitur baru ES6 ini digunakan untuk membentangkan nilai array atau lebih tepatnya iterable object menjadi beberapa elements.

const favorites = ["Seafood", "Salad", "Nugget", "Soup"];

console.log(...favorites);

// Sama seperti kita menuliskan kode seperti ini:
// console.log(favorites[0], favorites[1], favorites[2], favorites[3]);
